apiVersion: v1
kind: Service
metadata:
  name: mario-api-service
  labels:
    app: mario-api
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    app: mario-api
