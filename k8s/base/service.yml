apiVersion: v1
kind: Service
metadata:
  name: mario-api-service
  labels:
    app: mario-api
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: 5000
      protocol: TCP
  selector:
    app: mario-api
